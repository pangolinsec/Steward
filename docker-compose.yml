services:
  steward:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/data
    environment:
      - STEWARD_DATA_DIR=/data
      - PORT=3000
    restart: unless-stopped
