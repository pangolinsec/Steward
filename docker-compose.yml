services:
  almanac:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - almanac-data:/data
    environment:
      - ALMANAC_DATA_DIR=/data
      - PORT=3000
    restart: unless-stopped

volumes:
  almanac-data:
